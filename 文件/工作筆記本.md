# 工作筆記本

> **這是活的文件**——計畫改了就改這裡，不要另開新檔。
> `文件/` 底下其他帶日期的計畫檔（`後續計劃表.md`、`明日待辦_2026-08-30.md`…）
> 是當時的快照，**不要當成現行計畫**。
>
> 分工：`CLAUDE.md` 是與 Codex 的交接與證據帳，這裡是工作計畫與待辦。
> 最後更新 2026-09-01。

---

## 現在在做什麼

已完成：網路特徵四缺陷修正、來源位址偽造加固（含 live 驗證）、分層模型與
open-set 在封包表上重做、跨主機批次輪次對齊查核、攻擊面盤點。

**下一步**：`discovery_recon` 的攻擊腳本（離線可寫），見計畫第 2 項。

---

## 計畫（依順序）

### 1. 網路特徵修正 ✅ 完成

四個缺陷，一個帶出一個。完整記錄見
[網路特徵四個缺陷與修正_2026-08-31.md](網路特徵四個缺陷與修正_2026-08-31.md)。

| # | 缺陷 | 修法 |
|---|---|---|
| 1 | Zeek 沒加 `-C`，連線數虛增 7.96× | 1,400 場離線重建，不覆寫原始證據 |
| 2 | 按流起點分窗，一條 50 秒的流只落進一個視窗 | 逐封包分窗（tshark，11.6M 封包） |
| 3 | 放大比用五元組反轉配對，恆為零 | 改按主機計 |
| 4 | 比例特徵按五元組加權（**我引入的**） | 改按封包加權 |

重訓（validation only，舊表在新程式下逐位重現作為控制組）：

| | 舊表 | 封包表 | 差 |
|---|---:|---:|---:|
| Permissive balanced accuracy | 0.8823 | **0.9243** | **+0.042** |
| Permissive macro F1 | 0.8617 | **0.9174** | +0.056 |
| Permissive `normal` recall | 0.864 | **0.966** | +0.102 |
| Enforce balanced accuracy | 0.4674 | **0.4892** | **+0.022** |
| Enforce macro F1 | 0.4578 | **0.4867** | +0.029 |

Permissive 逐類全部持平或上升，升最多的兩類（`identity_abuse` +0.154、
`command_injection` +0.118）正是至今沒有專屬證據通道的那兩類。

⚠️ Enforce 的 `command_injection` 從 0.833 掉到 0.475，是唯一的大幅退步。
它在舊表是強烈離群值（次高只有 0.458），無法乾淨歸因；但可以確定它依賴的是
**依校驗和有效性挑出的封包子集**，而校驗和卸載是擷取主機網卡的性質，
換到 Pi 上會完全不同。不主張它是假的，但不可依賴。

⚠️ **final test 沒有開**——新舊表同一批場次、同一個切分 seed，而 test 已在
舊表上開過一次。上面全部是 validation。

剩下的：分層模型與 open-set 尚未在封包表上重做。

### 2. 攻擊面擴充 —— 盤點完成，**不要直接接上去跑**

完整盤點見
[攻擊面盤點與擴充風險_2026-09-01.md](攻擊面盤點與擴充風險_2026-09-01.md)。

先更正我自己講過的數字：**7 支 PoC 進了資料集**（不是 8），
`unauthorized_participant` 用的是 ROS demo talker 不是 PoC 腳本。

policy 有 23 條規則、模型只有 9 類。那 14 個空類別裡：

- **9 類已經有腳本**，其中 8 支介面直接相容（收 duration、exit 0）
- **5 類完全沒有腳本**：`discovery_recon`、`node_churn`、`odom_spoof`、
  `spdp_flood`（`cmd_vel_race` 的腳本被別的類別用掉了）
- ⚠️ 即使全部接上也只有 18 類，**`all_policy_classes_present` 這個 gate
  不可能靠這條路單獨打開**

⚠️⚠️ **最重要的發現：六支打的是已經修好的缺陷。** `N2`／`N5`／`N4`／`N20`／
`N24b`／`N8` 的 docstring 自己寫著「已修補」「修補加了」「回歸測試」。漏洞修好
之後再跑，很可能**完全沒有應用層訊號**——而「攻擊跑了沒效果」與「防禦擋下了」
在資料上長得一模一樣。

直接接上去跑會複製 C2C-013 記錄的失效：新增的類別模型認不出來，balanced
accuracy 被拖下去，而且看起來像「類別太多所以變難」。

**所以擴充前要有一道 gate**：每支候選先跑 1 場 smoke，判準是「至少一個特徵
在該場非零、而在 normal 基線恆零」。9 場，遠低於一輪 campaign 的 15 小時。

預期（跑完對照）：**打已修補缺陷的那六支多半不會通過。** 若反而通過了，
代表那些漏洞沒真的修好——那是更重要的發現。

### 3. `discovery_recon` 被動偵察 ⬅ **下一步**

它是 14 個空類別裡唯一**既沒有腳本、policy 裡卻有規則、而且現有特徵結構上
看不見**的那一類。

八個既有攻擊**全部會送流量**，而 14 個網路特徵全部是速率／比例／連線數——
**沒有一個看得見一個不說話的攻擊者**。純被動的偵察者只監聽 SPDP／SEDP、
繪製拓撲、不送任何東西，產生 0 個異常封包。

它唯一會留下的痕跡是 **participant 的存在本身**——也就是身份層，正是 8/31
補起來的那條通道。**所以這不只是補一個類別，它是那條新通道唯一能證明價值
的場景。** 排在任何回歸 PoC 之前。

腳本可以離線寫；收資料要 live 授權。

### 4. 來源位址偽造的加固 ✅ 完成

完整記錄見 [來源位址偽造加固_2026-08-31.md](來源位址偽造加固_2026-08-31.md)。

封鎖判定加上**第四條：鏈路層綁定**。防守方送出時的 `eth.dst` 是它自己的 ARP
解析結果，收到時的 `eth.src` 是實際發送者——偽造來源時兩者必然不同，因為
回應會被送到真正持有那個 IP 的主機。完全被動，不需要送任何封包。

**回頭用新規則驗證 8/30 那批：80 場仍然成立、零撤回。** 那批結論在更嚴的
規則下站得住，而且現在更強。每場的證據已落地為 `link_layer_binding.json`。

三級判定而不是二元：`spoofing_evidence`（確證）與 `unverifiable`（分不出來）
分開，因為防守方自己的位址會落在後者——WSL mirrored 讓同一個 IP 同時出現
實體與虛擬網卡的 MAC。判定一樣，但理由不能編。

⚠️ **沒有實際做過偽造攻擊。** 偽造情境是單元測試的合成資料。只能說規則會擋下
具備該特徵的輸入，不能說已在真實攻擊下驗證——那需要在第二台主機上執行來源
位址偽造，是新的 live 授權工作。**這仍然是滲透測試最該先打的目標。**

### 5. 攻擊參數化

9 個 scenario 只有 2 個真的用到 `intensity`（`oversized_scan`、
`parameter_flood`），其餘 7 個在 `scenarios.json` 宣告了範圍但 runner 忽略。
所以 7 類的 100 場在參數空間裡是同一個點重複 100 次。

⚠️ 做完**識別率很可能下降**——現在的 0.8619 有一部分是「攻擊每次都一樣所以
好認」換來的。那是誠實的下降，但如果目標是分數，這一項是反方向的。

### 6. 需要授權或硬體的

| 項目 | 需要 |
|---|---|
| 九類全量帶觀測者重跑（約 11.5 小時） | live 授權 |
| 限流回應階層 | 部署姿態決定 |
| kernel nftables 真跑 | root |
| Pi 5 驗收 | 硬體 |

---

## 已完成（本輪）

- **8/29–30 跨主機 79／80**，79 個相異 GUID，陰性對照零誤判，8 小時零變異
- **8/30 本機 outcome 8／9**（本機天花板）
- **8/31 `sros_auth_fail_rate` 第一次有來源**：sidecar 觀測者繞過 rmw，
  100 場驗證 normal=0／identity_abuse=18，零變異。**那是 Enforce 下唯一非零的
  遙測特徵**
- **8/31 Zeek `-C` 缺陷**：見上

## 不要重犯的錯

| 錯 | 教訓 |
|---|---|
| 觀測者釘傳輸 → 合法節點全被判 UNAUTHORIZED | 同機**不可**釘；跨主機**必須**釘。沒有安全預設值 |
| campaign 不啟動 stack | 它假設 external stack，少了就 `not_eligible:runtime_telemetry` |
| 只重跑受益類別 | 會造出「哪些場次有觀測者」的假象提升。用 `check_channel_coverage.py` 擋 |
| 用平均值比較兩臂 | 一個離群值就能編出不存在的擾動。用中位數 |
| 警告而不阻擋 | 觀測者沒起來只印警告 → 壞資料留在結果裡。要作廢那一對 |
| Zeek 每場都警告校驗和，沒有人讀 | 工具的 stderr 有存進 `zeek_process.json`，但沒有任何檢查看它 |
| 修好資料後表變小就以為修壞了 | 舊表的時間解析度是 bug 的副產物；先問「舊的為什麼會那樣」 |
| 用五元組反轉配對回流 | RTPS 回應來自臨時埠，實測 462 個五元組裡嚴格反轉配對 0 個 |

---

## 2026-09-01 下一步計畫

### 現況查核（今天實查，不是引用）

- 出貨 catalog **10 個 scenario，全部是未認證的外部者**。內鬼（N29／N30）
  由 `run_local_outcomes.sh` 驅動，不在 catalog 裡。
- 部署 gate 三紅：`all_policy_classes_present`、
  `minimum_1100_independent_sessions`、`validated_live_multimodal_contract`。

### 順序

**① 內鬼對照（進行中）**

把 N29／N30 接成 catalog scenario，跑 40 場（2 類 × 20）帶觀測者，量身份通道
對持合法憑證攻擊者的沉默程度。

理由：`身份通道對未見攻擊的效果_2026-09-01.md` 第三節寫著「對內部威脅無效」，
那目前**是斷言不是量測**。完整的界線需要兩個數字：

    外部未授權：recall 0.99、誤報 0.00   ← 已量到
    內部持證  ：應為 0                   ← 未量

兩種結果都有價值。真的是零 → 界線乾淨可寫進論文；不是零 → 我的說法要改。

成本：離線約 3 小時（runner 接線＋測試＋catalog 註冊，需登記
`ARCHIVED_COMPLETED_CATALOGS`），live 約 40 分鐘（需 Jesse 當次授權）。

**② 三個部署 gate 的處置（需 Jesse 決定，不自行更動）**

兩個紅燈不是模型品質問題：

- `minimum_1100_independent_sessions`：分模式後每邊 550，是**分模式的副作用**。
  這條 gate 是為 pooled 模型寫的。
- `validated_live_multimodal_contract`：契約要求安全模式不可從網路特徵還原，
  但實測 separability **0.9451**——而那個發現**正是改採分模式的理由**。
  **這個 gate 在否決一個它自己促成的架構。** C2C-014 提過，未結論。

動的是通過標準，所以不自己決定。

**③ 接兩支候選攻擊進出貨 catalog** — `cross_channel_relay`、`scan_drift`，
離線 30 分鐘，攻擊面 +2 類。

**④ 報告合併（累積中的債）**

最近一份完整報告是 **8/21**。之後的重大結果一份都沒進去：

| 結果 | 日期 |
|---|---|
| 跨主機 79／80 ＋ 8 小時 soak | 08-30 |
| 來源位址偽造加固 ＋ N31 live | 08-31／09-01 |
| 四個網路特徵缺陷與修正 | 08-31 |
| 本機防禦 8／9（本機天花板） | 08-29 |
| 身份通道對未見攻擊的效果 | 09-01 |

十一天、五項主要結果、零份合併文件。

### 2026-09-02 進度

**① 內鬼對照 — 完成。** 80 場、四類各 20、Enforce、61 分鐘。
持證內鬼身份層 **0／40**；第二層 764 次 `invalid_signature` ＋ 279 次
`rcl_read_only` 證明攻擊確實抵達並被擋下；正向對照 20／20 橫跨全程、
每場都是 18、零變異。特徵層內鬼 473 個視窗全零。
見 `文件/身份通道對持證內鬼無效_2026-09-01.md`、C2C-053。

**③ 候選升級 — 完成。** `cross_channel_relay`、`scan_drift` 進出貨 catalog。
gate 判定重跑過（不是引用上次摘要）：專屬訊號基線恆零、兩兩共用 0。
順帶清掉 `discovery_recon` 在候選檔的殘留，加 7 個測試守住。
policy 類別覆蓋 **9 → 12 → 14**。

**② 三個部署 gate — 一項完成，兩項等 Jesse。**
`3a` 更新過期的 blocker 文字已做（`aaebe45`，門檻未動、diff 一行）。
`2a`（1,100 → 每模式 550）與 `3b`（移除模式洩漏要求）都是**放寬通過標準**，
不自行決定。選項在 `文件/三個部署Gate的處置_待決定_2026-09-01.md`。

**④ 報告合併 — 未開始，債還在長。** 最近一份完整報告仍是 8/21，
之後現在有**七項**主要結果沒進任何合併文件（新增內鬼對照與候選升級）。

#### 下一步的順序

| 項目 | 需要什麼 |
|---|---|
| ④ 報告合併 | 離線。目前最大的一塊債 |
| 剩餘 9 個 policy 類別 | 5 類有候選但沒過 gate、4 類無 runner |
| `2a`／`3b` | **Jesse 決定** |
| 跨主機正向對照 | 需 Jesse 授權＋設計決定（第二台放合法 enclave 與現行威脅模型界線衝突） |

### 2026-09-02 決定：兩項放寬都不做

Jesse 決定 `2a`（`MIN_DEPLOYMENT_SESSIONS` 1,100 → 每模式 550）與
`3b`（移除「安全模式不可從網路特徵還原」）**都不執行**。

理由不是保守，是查證的結果：`3b` 的前提**不成立**。那份 contract 的第二條
blocker 警告「已找到兩個實驗室缺陷，不可假設沒有第三個」——**第三個確實存在**：
`172.30.123.103` 在 Permissive 一次都沒出現，光看來源位址就能分辨模式，
而那是 WSL mirrored 特有的。見 `模式可分離度的來源位址混淆_2026-09-02.md`。

**這一項不要再重開**，除非在單介面硬體（Pi 5）上重新量過模式可分離度。

⚠️ 順帶記一個必須寫進硬體驗收前提的事：**Enforce 現有的識別數字依賴這個
實驗室的網路拓撲。** 只留 loopback 列重訓，validation 從 0.4681 掉到 0.3160。
Pi 5 上合理預期接近 0.316 而不是 0.4155——到時量到退步不要誤讀成移植出問題。

#### 本輪（09-01 → 09-02）完成的

| 項目 | 結果 |
|---|---|
| ① 內鬼對照 | 80 場，內鬼身份層 0／40，第二層 1,043 次拒絕 |
| ② gate 處置 | `3a` 做了；`2a`／`3b` 決定不做 |
| ③ 候選升級 | policy 類別覆蓋 9 → 14 |
| ④ 報告合併 | `階段成果合併_2026-08-22至09-02.md` ＋ artifact |

進度：整體 78% → **76%**（新增「攻擊面覆蓋」軸）。測試 911 passed。

#### 還沒做的

| 項目 | 需要什麼 |
|---|---|
| 剩餘 9 個 policy 類別 | 5 類有候選但沒過 gate、4 類無 runner |
| 身份特徵整合進正式表 ＋ 重做 family-LOO | 離線，但要先決定怎麼處理來源位址混淆 |
| 跨主機正向對照 | 授權＋設計決定（第二台放合法 enclave 與現行威脅模型界線衝突） |
| Pi 5 與 kernel nftables | 授權＋硬體 |

### 2026-09-02 夜間（Jesse 睡覺時）

**做完的**

| 項目 | 結果 |
|---|---|
| 查 `3b` 的前提 | **不成立**，兩個放寬都不做（見下） |
| 模式可分離度的成因 | 是**加密的物理效果**，不是來源位址混淆——我更正了自己稍早的說法 |
| C2C-050 欠的 family-LOO 重做 | 四組全部重跑，permissive/Maha 0.2709 → **0.4507** |
| `hmac_result.channel` 遙測欄位 | 生產端必填、驗證端選填 |
| gate 的**欄位配對**編碼 | `health_spoof` 與 `mission_spoof` 因此可分 |
| 攻擊面覆蓋 | 14 → **16／23**，整體 76% → **77%** |
| 待辦表整理 | 刪兩項死條目、一個候選；**沒刪任何文件檔案** |

**兩個我自己的更正**

1. 「來源位址混淆佔 0.9451 相當一部分」——**錯**。限制在共有 loopback 反而
   升到 AUC 1.0，訊號在 `mean_bytes_per_packet`（每封包多 245 位元組）。
2. 「今天重訓了分層模型」——**沒有**。那批是 08-31 的，我今天的指令撞到
   fail-closed 保護什麼都沒做，而我沒讀它的輸出。

**一個先前完全沒被報告的結果**

官方 holdout 的 open-set recall：Permissive **0.5789 → 0.2105**、
Enforce 0.8562 → 0.9125。而且**那個 holdout 已被花第三次**，
兩個數字都不是獨立估計，只能當診斷。未知攻擊維持 70%。

**三個量測缺陷（smoke 跑三次，前兩次作廢，全是我的）**

1. 啟動 stack 前沒 export `FIREWALL_LIVE_RUNTIME` → socket 建在 9p 上失敗
   → 每場只剩 45 行 tick（對照 5,514 行）→ gate 判「沒有專屬訊號」。
   **第九次「量測工具偽裝成被觀測系統」，而我同一天才把這形態寫進報告。**
2. fail-fast 在啟動探針前檢查 socket（那是 collector 綁的）。
3. smoke 腳本的候選陣列寫死，與檔案分岔後 4 支被跳過而 rc 仍是 0。已改成
   從 catalog 讀。

**⚠️ 醒來要決定的**

- Pi 5 硬體：27W 電源、主動散熱、有線網路、NVMe 或 A2 級 SD。
  **不要打算在 Pi 上跑 Gazebo**——它是防火牆不是模擬器。
- 要不要清文件檔案。我只清了表格與候選，**沒動任何 `.md`**——
  證據總帳釘著路徑與 SHA-256，刪掉會讓帳本驗不過。要清請單獨說，
  我會先列出哪些被釘住。
