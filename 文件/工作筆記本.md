# 工作筆記本

> **這是活的文件**——計畫改了就改這裡，不要另開新檔。
> `文件/` 底下其他帶日期的計畫檔（`後續計劃表.md`、`明日待辦_2026-08-30.md`…）
> 是當時的快照，**不要當成現行計畫**。
>
> 分工：`CLAUDE.md` 是與 Codex 的交接與證據帳，這裡是工作計畫與待辦。
> 最後更新 2026-08-31。

---

## 現在在做什麼

第一項（網路特徵修正）已完成，見下。**下一步是第 2 項或第 3 項**——
兩者都需要 live 授權才能收新資料，但離線的準備工作可以先做。

---

## 計畫（依順序）

### 1. 網路特徵修正 ✅ 完成

四個缺陷，一個帶出一個。完整記錄見
[網路特徵四個缺陷與修正_2026-08-31.md](網路特徵四個缺陷與修正_2026-08-31.md)。

| # | 缺陷 | 修法 |
|---|---|---|
| 1 | Zeek 沒加 `-C`，連線數虛增 7.96× | 1,400 場離線重建，不覆寫原始證據 |
| 2 | 按流起點分窗，一條 50 秒的流只落進一個視窗 | 逐封包分窗（tshark，11.6M 封包） |
| 3 | 放大比用五元組反轉配對，恆為零 | 改按主機計 |
| 4 | 比例特徵按五元組加權（**我引入的**） | 改按封包加權 |

重訓（validation only，舊表在新程式下逐位重現作為控制組）：

| | 舊表 | 封包表 | 差 |
|---|---:|---:|---:|
| Permissive balanced accuracy | 0.8823 | **0.9243** | **+0.042** |
| Permissive macro F1 | 0.8617 | **0.9174** | +0.056 |
| Permissive `normal` recall | 0.864 | **0.966** | +0.102 |
| Enforce balanced accuracy | 0.4674 | **0.4892** | **+0.022** |
| Enforce macro F1 | 0.4578 | **0.4867** | +0.029 |

Permissive 逐類全部持平或上升，升最多的兩類（`identity_abuse` +0.154、
`command_injection` +0.118）正是至今沒有專屬證據通道的那兩類。

⚠️ Enforce 的 `command_injection` 從 0.833 掉到 0.475，是唯一的大幅退步。
它在舊表是強烈離群值（次高只有 0.458），無法乾淨歸因；但可以確定它依賴的是
**依校驗和有效性挑出的封包子集**，而校驗和卸載是擷取主機網卡的性質，
換到 Pi 上會完全不同。不主張它是假的，但不可依賴。

⚠️ **final test 沒有開**——新舊表同一批場次、同一個切分 seed，而 test 已在
舊表上開過一次。上面全部是 validation。

剩下的：分層模型與 open-set 尚未在封包表上重做。

### 2. 攻擊面擴充

紅隊寫了 **25 個攻擊腳本，資料集只用了 8 個（32%）**。優先加質性不同的：

| 優先 | 攻擊 | 測什麼防禦層 |
|---|---|---|
| 1 | N29 內鬼（有憑證、無 HMAC 金鑰） | **Enforce 下第一個有應用層訊號的攻擊** |
| 2 | N23 白名單名字＋純行為觸發 | 繞過身分檢查，只能靠行為 |
| 3 | N4 跨頻道簽章混淆 | HMAC 頻道綁定 |
| 4 | N21 用 IDS 自己誘發 DoS | 攻擊者借防禦之手（SmartDDoS） |

⚠️ 加入 N29／N30 會**改變威脅模型敘事**（從「純外部攻擊者」變成「也涵蓋憑證
被竊」），論文要重新定義，這是研究設計決定不是工程決定。

### 3. 被動偵察攻擊（目前覆蓋率 0）

八個攻擊**全部會送流量**。純被動的攻擊者只監聽 SPDP／SEDP、繪製拓撲、不送任何
東西，會產生 0 個異常封包，而 14 個網路特徵**全部是速率／比例**，沒有一個看得見
不說話的攻擊者。

這是「小量間接型攻擊」的極端情況，而且它唯一會留下的痕跡是 participant 的存在
本身——也就是身份層，正是 8/31 補起來的那條通道。**這個缺口會讓那條通道的價值
第一次被量到。**

### 4. 來源位址偽造的加固（威脅已寫下的結論）

同網段攻擊者偽造來源 IP 為受害者 → 握手必失敗 → 觀測者記 UNAUTHORIZED →
封包層把 GUID 綁到受害者 IP → **三條判定全過，宣告無辜主機可封鎖**。

8/30 的 79／80 陰性對照測不到（對照組是防守方自己，不是被偽造的第三方）。

鑑別方式已知：真攻擊者有 156 個分片與憑證交換，偽造來源的單向攻擊者握手停在
第一步、不會有憑證流量。判定要加「雙向流量與憑證交換」——**目前沒有檢查**。

**這是滲透測試最該先打的目標。**

### 5. 攻擊參數化

9 個 scenario 只有 2 個真的用到 `intensity`（`oversized_scan`、
`parameter_flood`），其餘 7 個在 `scenarios.json` 宣告了範圍但 runner 忽略。
所以 7 類的 100 場在參數空間裡是同一個點重複 100 次。

⚠️ 做完**識別率很可能下降**——現在的 0.8619 有一部分是「攻擊每次都一樣所以
好認」換來的。那是誠實的下降，但如果目標是分數，這一項是反方向的。

### 6. 需要授權或硬體的

| 項目 | 需要 |
|---|---|
| 九類全量帶觀測者重跑（約 11.5 小時） | live 授權 |
| 限流回應階層 | 部署姿態決定 |
| kernel nftables 真跑 | root |
| Pi 5 驗收 | 硬體 |

---

## 已完成（本輪）

- **8/29–30 跨主機 79／80**，79 個相異 GUID，陰性對照零誤判，8 小時零變異
- **8/30 本機 outcome 8／9**（本機天花板）
- **8/31 `sros_auth_fail_rate` 第一次有來源**：sidecar 觀測者繞過 rmw，
  100 場驗證 normal=0／identity_abuse=18，零變異。**那是 Enforce 下唯一非零的
  遙測特徵**
- **8/31 Zeek `-C` 缺陷**：見上

## 不要重犯的錯

| 錯 | 教訓 |
|---|---|
| 觀測者釘傳輸 → 合法節點全被判 UNAUTHORIZED | 同機**不可**釘；跨主機**必須**釘。沒有安全預設值 |
| campaign 不啟動 stack | 它假設 external stack，少了就 `not_eligible:runtime_telemetry` |
| 只重跑受益類別 | 會造出「哪些場次有觀測者」的假象提升。用 `check_channel_coverage.py` 擋 |
| 用平均值比較兩臂 | 一個離群值就能編出不存在的擾動。用中位數 |
| 警告而不阻擋 | 觀測者沒起來只印警告 → 壞資料留在結果裡。要作廢那一對 |
| Zeek 每場都警告校驗和，沒有人讀 | 工具的 stderr 有存進 `zeek_process.json`，但沒有任何檢查看它 |
| 修好資料後表變小就以為修壞了 | 舊表的時間解析度是 bug 的副產物；先問「舊的為什麼會那樣」 |
| 用五元組反轉配對回流 | RTPS 回應來自臨時埠，實測 462 個五元組裡嚴格反轉配對 0 個 |
