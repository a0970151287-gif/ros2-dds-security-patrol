# ROS2 DDS 安全監控系統 — 架構規格書

> 本文件為系統的「藍圖」
> **Topic 定義是什麼？裡面傳了什麼？誰發布？誰接收？掛掉會怎樣？**
>
> **版本：TQC + HMAC envelope v3 + 行為 IDS。** 最新完整規格見
> [紅隊測試/ARCHITECTURE.md](../../紅隊測試/ARCHITECTURE.md) 與
> [紅隊測試/系統威脅分析.md](../../紅隊測試/系統威脅分析.md)。

---

## 節點總覽

| 節點名稱 | 功能 | 發布 Topic | 訂閱 Topic |
|---------|----------|-----------|-----------|
| `monitor_node` | 掃描節點圖偵測未知節點 + 簽章發警報/心跳 | `/security/alerts`（簽章）`/security/heartbeat`（簽章） | DDS graph（直接 API） |
| `intelligent_defense_node` | 行為 IDS（D1–D6）+ cascade 斷路器 | `/security/alerts`（簽章） | `/cmd_vel` `/scan` `/odom` `/security/heartbeat` |
| `patrol_node` | 幾何巡邏控制（部署模式） | `/cmd_vel` | `/scan` `/odom` `/security/alerts` `/patrol/goto`（簽章） |
| `burger_sac_env` | TQC 訓練/推論環境 | `/cmd_vel` | `/scan` `/odom` `/security/alerts` |
| `sensor_hub_node` | 彙整感測器資料 | `/sensor/status`（簽章 CH_SENSOR） | `/scan` `/imu` |
| `mission_manager_node` | 決定機器人任務狀態 | `/mission/cmd`（簽章 CH_MISSION） | `/sensor/status` `/security/alerts` |
| `system_status_node` | 彙整所有狀態輸出報告 | `/system/health`（簽章 CH_HEALTH） | `/sensor/status` `/mission/cmd` `/security/alerts` |

---

## Topic 詳細規格

### `/scan`
- **型別**：`sensor_msgs/msg/LaserScan`
- **來源**：Gazebo（透過 ros_gz_bridge 橋接）
- **內容**：360 個方向的距離值（0~3.5m），angle_min/max, ranges[]
- **頻率**：~5 Hz（每 0.2 秒一筆）
- **訂閱者**：`patrol_node`（判斷障礙物）、`sensor_hub_node`（計算最近距離）
- **掛掉影響**：patrol_node 停止迴避動作，sensor_hub_node 發出「等待 LiDAR」，機器人可能撞牆

---

### `/imu`
- **型別**：`sensor_msgs/msg/Imu`
- **來源**：Gazebo（透過 ros_gz_bridge）
- **內容**：linear_acceleration (x, y, z)，angular_velocity
- **頻率**：~100 Hz
- **訂閱者**：`sensor_hub_node`（計算水平加速度）
- **掛掉影響**：sensor_hub_node 顯示加速度 0.00，不影響核心安全功能

---

### `/cmd_vel`
- **型別**：`geometry_msgs/msg/TwistStamped`
- **來源**：`patrol_node`（幾何巡邏）或 `burger_sac_env`（TQC AI 控制）。緊急停止由各下游模組**驗章後自行送 0 速度**，不再由 monitor 直接覆蓋 `/cmd_vel`
- **內容**：
  - `twist.linear.x`：前進速度（m/s），正常巡邏 0.20 m/s，停止時 0.0
  - `twist.angular.z`：轉向速度（rad/s），-0.8 ~ +0.8
  - `header.frame_id`：`"base_link"`
- **頻率**：patrol_node 以 10 Hz 發布（每 0.1 秒），monitor_node 緊急停止時 10 Hz
- **訂閱者**：機器人底層驅動（turtlebot3_node，透過 ros_gz_bridge 送進 Gazebo）
- **掛掉影響**：機器人繼續執行最後一道指令（可能持續移動），**無指令 ≠ 停止**
- **資安意義**：攻擊者若能發布此 Topic，可直接劫持機器人移動方向

---

### `/security/alerts`
- **型別**：`std_msgs/msg/String`（內含 **HMAC envelope v3 簽章**，非純文字）
- **來源**：`monitor_node`（節點偵測）、`intelligent_defense_node`（行為偵測）
- **內容**：envelope v3 — `{"body":"{channel:alerts, nonce, payload, ts}", "sig":"<HMAC-SHA256>"}`，payload 為警報文字
- **頻率**：事件驅動（發現異常才發一次），非固定頻率
- **訂閱者**：`patrol_node`、`mission_manager_node`、`system_status_node`、`burger_sac_env` —— **皆先 `verify_alert` 驗章（五道檢查）通過才動作**
- **QoS**：RELIABLE + VOLATILE + max_age 3s（防重放；anti-replay 由 ts + nonce + ReplayCache 保證）
- **掛掉影響**：系統喪失安全警報能力；但攻擊者也無法偽造（無密鑰簽不出 sig）

---

### `/sensor/status`
- **型別**：`std_msgs/msg/String`
- **來源**：`sensor_hub_node`
- **內容**：格式化文字，例如：
  - `"[感測器狀態] 最近障礙物: 0.45m | ✅ 安全 | 水平加速度: 0.02m/s²"`
  - `"[感測器狀態] 最近障礙物: 0.22m | ⚠️ 危險 | 水平加速度: 0.15m/s²"`
- **頻率**：1 Hz（每 1 秒一筆）
- **訂閱者**：`mission_manager_node`（判斷是否切換 AVOID_OBSTACLE）、`system_status_node`（健康報告）
- **掛掉影響**：mission_manager 停留在最後狀態，不再自動切換任務模式

---

### `/mission/cmd`
- **型別**：`std_msgs/msg/String`
- **來源**：`mission_manager_node`
- **內容**：任務字串，固定三種值：
  - `"PATROL"` — 正常巡邏
  - `"AVOID_OBSTACLE"` — 障礙物迴避
  - `"EMERGENCY_STOP"` — 安全警報緊急停止
- **頻率**：狀態改變時發布，最快 1 Hz（recovery timer 觸發頻率）
- **訂閱者**：`system_status_node`（顯示任務狀態）
- **掛掉影響**：system_status_node 不再更新任務狀態，但 patrol_node 不受影響（patrol_node 直接訂閱 /security/alerts）

---

### `/system/health`
- **型別**：`std_msgs/msg/String`
- **來源**：`system_status_node`
- **內容**：格式化健康報告，例如：
  ```
  ========== 系統健康報告 ==========
  感測器: [感測器狀態] 最近障礙物: 0.45m | ✅ 安全 | 水平加速度: 0.02m/s²
  任務:   PATROL
  資安:   ✅ 安全
  ==================================
  ```
- **頻率**：2 Hz（每 2 秒一筆）
- **訂閱者**：無（供操作者監看，終端顯示用）
- **掛掉影響**：喪失系統總覽，但各節點功能不受影響

---

## 節點掛掉影響分析

| 掛掉的節點 | 立即影響 | 是否危及安全 |
|-----------|---------|------------|
| `monitor_node` | 無法偵測未知節點，無 LINE 警報 | ⚠️ **是**，喪失核心偵測能力 |
| `patrol_node` | 機器人停止移動（最後指令執行完後停） | 低（機器人停止） |
| `sensor_hub_node` | mission_manager 停留最後狀態，不切換 AVOID_OBSTACLE | 中（可能撞牆） |
| `mission_manager_node` | 不影響巡邏和安全警報，失去任務狀態顯示 | 低 |
| `system_status_node` | 喪失集中儀表板，各節點正常運作 | 低 |

---

## 資料流向圖

```
Gazebo 模擬器
  ├─ /scan (5Hz) ──────────────┬──→ patrol_node ──→ /cmd_vel ──→ 機器人
  │                            └──→ sensor_hub_node
  └─ /imu (100Hz) ─────────────────→ sensor_hub_node

sensor_hub_node ──→ /sensor/status (1Hz) ──→ mission_manager_node ──→ /mission/cmd
                                          └──→ system_status_node

monitor_node ─────┐ (簽章)
intelligent_      ├──→ /security/alerts (envelope v3) ──→ patrol_node（驗章→停止）
defense_node ─────┘                                  ├──→ mission_manager_node（EMERGENCY_STOP）
                                                     ├──→ system_status_node（顯示警報）
                                                     └──→ burger_sac_env（終止 episode）
   各下游驗章通過後自行送 /cmd_vel = 0（resume 30s 固定 + cascade quiet window）

monitor_node ──→ /security/heartbeat (2Hz, 簽章) ──→ intelligent_defense_node（D5 watchdog）

system_status_node ──→ /system/health (2Hz, 簽章) ──→ 終端顯示 / self-watch
```

---

## 給教授的回答：Topic 定義是什麼？

**Topic 是 ROS2 的發布/訂閱通訊頻道**，類似廣播電台：
- 發布者（Publisher）：把資料「廣播」到這個頻道
- 訂閱者（Subscriber）：「收聽」這個頻道的資料
- **任何節點都可以訂閱，但誰有權限「發布」由 permissions.xml 控制**

在本系統的情境裡（⚠️ 重要：本系統 SROS2 為 **Permissive 模式**，DDS 層**不**強制擋發布）：

- `/cmd_vel`（速度指令）：訊息型別非 String，無法包 HMAC 簽章。在 Permissive 模式下，攻擊者**可以**發布 `/cmd_vel`（DDS 不擋）。防線改在**行為層**：`intelligent_defense_node` 用 D1 物理門檻（>0.23 m/s）、D4 unauthorized publisher、D6 cmd-vs-odom 一致性偵測注入。**根治需切到 SROS2 Enforce 模式**（列入 90 天計畫）。
- `/security/alerts`（警報）：靠 **應用層 HMAC envelope v3 簽章**防偽造，不是靠 DDS 權限。攻擊者就算能發布（Permissive 不擋），沒有 `alert_secret` 也簽不出合法 sig，接收端 `verify_alert` 一律拒絕。

> **教授若追問「DDS 層有沒有擋」：誠實答「Permissive 模式 DDS 層不擋，我們用應用層簽章 + 行為 IDS 補；要 DDS 層強制擋需切 Enforce，這是未來工作」。** 不要說「DDS 會擋下」——那是 Enforce 模式才成立，本系統目前是 Permissive。
