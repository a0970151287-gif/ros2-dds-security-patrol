# 明天早上要做的事（2026-08-30）

> 2026-08-29 深夜寫下。當晚跑著一個 8 小時的跨主機批次收集，睡醒先看它。

## 昨晚的狀態（給睡醒的自己，或給新的 session）

- **跨主機身份歸因首次成立**（session `…76a52ce0`）。wrong-CA participant 在 DDS
  認證層被拒，封包層把同一 GUID 綁到 `192.168.0.30`，兩半交集 1，
  `source_ip_attribution_verified=true`——1,101 場既有資料集裡這欄一直是 false。
  詳見 `文件/跨主機身份歸因首次成立_2026-08-29.md`。
- **本機九項 outcome 達 8／9**，天花板（第 9 項 `replay_dropped` 的阻塞本身
  就是防禦有效）。
- 整體進度 **76%**。commit 到 `da3dc21`。
- 環境：WSL `networkingMode=mirrored`，防守端 `192.168.0.129`（介面 **每次都要
  用 `dumpcap -D` 現看**，一晚之內從 eth2 變成 eth1），攻擊機桌機 `192.168.0.30`。
  Hyper-V 防火牆規則 `SROS2-DDS-Domain30` 放行 UDP 14900-14999。

---

## 1. 先看批次結果（第一件事）

```bash
wsl -d Ubuntu-24.04 bash -lc "tail -50 /tmp/crosshost_batch.log"
```

彙總的四個數字：

| 指標 | 期望 | 不如預期時 |
|---|---|---|
| attestation 成立輪數 | 越多越好 | 看是不是攻擊端迴圈提早結束 |
| 攻擊者 IP 判為可封鎖 | 等於上一列 | 有落差要逐輪查 |
| **誤把其他 IP 判為可封鎖** | **必須是 0** | 非 0 是嚴重問題，判定規則有洞 |
| 攻擊者每輪 GUID 數 | 都是 `[1]` | 出現 2 代表一個窗內落入兩次攻擊 |

⚠️ 批次有兩道自動停止：區網位址消失、或連續 6 輪沒有 attestation。**如果它提早
停了，先看它印的理由**，不要當成「防禦一直在擋」。

## 2. 把這批證據記錄成正式產物

- 寫一份批次結果文件（重複性、時間分布、soak 觀察：觀測者連續八小時有沒有退化）
- 更新 `CLAUDE.md` 活狀態表的跨主機那一列（目前 25%，看結果決定是否上調）
- 附一則 C2C 訊息給 Codex

## 3. 把身份特徵接進模型（**今天最高價值的離線工作**）

昨晚拿到的是「證據取得成立」，**不是「模型變好」**。Enforce 識別 0.4155 與
處女 holdout 0.0273 目前**完全沒有改善**。

要做：

1. 把 identity 特徵接進特徵管線（Codex 的 `firewall_lab/identity_attribution.py`
   已定義 9 個 development-only 特徵，接口是現成的）
2. 重做 family-LOO
3. 看那兩格能不能動

⚠️ **先講清楚期待**：跨主機只有幾十場，而既有訓練集是 1,100 場同機資料。幾十場
不可能重訓，只能做「這些特徵在跨主機場次上有沒有區別力」的探索性分析。要真的
改善那兩格，需要跨主機重跑一整批 campaign——那是十幾小時的 live 工作，且需要
兩台都在。

## 4. 研究 Remote Control，讓兩台的 Claude 直接互傳訊息

**Jesse 明確表示有興趣。** 昨晚整晚的瓶頸就是「開窗 → 人工轉述 → 執行」，
四輪花了一個半小時才拿到一場證據；最後是靠兩邊各跑迴圈、只握手一次繞過去的。

已知的部分：`ListAgents` 的說明提到，當 Remote Control 在本機連上時，會列出
「你帳號的其他 session——其他機器上的 Remote Control session」，接著就能用
`SendMessage` 對它發訊息。昨晚實測 `ListAgents` 回報「no reachable agents」，
所以目前**沒有**連上。

⚠️ **我不知道確切的啟用步驟，不要憑印象照做。** 明天先查證再動手——可以問
`claude-code-guide`（它專門回答 Claude Code 的功能與設定問題），或查官方文件。

值得做的理由：跨主機實驗本質上需要兩邊協調，而人工轉述既慢又容易錯過視窗
（昨晚三輪都是這樣掉的）。但要注意威脅模型——**攻擊機上不可以有防守方的
keystore、私鑰或 enclave**，這條界線不因為多了一條控制通道而放寬。

## 5. 其餘待辦（沒有時間壓力）

- 以現行資料另出新 revision evidence ledger（C2C-037 P3 第 3 項）
- kernel nftables 真跑（需 root，且是部署姿態決定，要問 Jesse）
- Pi 5 驗收
- 論文收尾

---

## 不要忘記的邊界

- 跨主機一場成功 **不等於** 可以自動封鎖。`authorizes_action=false`、
  `executable_classes` 仍為空。
- 既有 1,101 場**不會**因此變成有 attestation。
- 昨晚的成功是同網段 Wi-Fi、多播不通（實測 0/25）、靠 unicast initial peer。
  換網路環境要重驗。
