# P1／P2 以現行資料重出（2026-08-28）

> C2C-036 發現 P1 的四份 LOO 與 P2 的兩份 conformal artifact，用的是
> **300 場重跑之前**的特徵表與 `.codex_tmp` 舊 metrics。當時我只回報，
> 沒有重出。本文補上。
>
> **Codex 的原始 artifact 一份都沒有被覆寫**，全部保留為歷史 checkpoint。
> 新結果寫在 `_2026-08-28_現行資料` 目錄。

## 一、舊 artifact 用了什麼

`文件/P1_AI平行閘門_LOO_2026-08-25/permissive_mahalanobis.json` 自己記著：

```
features         : firewall_lab/features_per_mode/fusion_features_permissive.csv
training_metrics : .codex_tmp/hierarchical_v2_20260817_r4/permissive/training_metrics.json
```

那份特徵表是 3,198 列，`parameter_call_rate` 與 `nonce_reuse_ratio` **非零率皆為 0.00%**
——正是 300 場重跑要修的兩條證據通道還全為零的版本。現行的
`features_merged_split` 是 4,397 列，兩者分別為 24.15% 與 2.32%，
分層模型 `models_hier` 也是用後者訓練的。

## 二、P1 平行閘門 LOO：結論不變，數字全變

| 設定 | 舊（8/16 前資料） | **新（現行資料）** | 翻面的約束旗標 |
|---|---:|---:|---|
| permissive／isolation_forest | 0.1352 | **0.0931** | `known_attack_false_unknown_budget`、`normal_false_unknown_budget` |
| permissive／mahalanobis | 0.2182 | **0.2709** | `normal_false_unknown_budget` |
| enforce／isolation_forest | 0.1049 | **0.0740** | 無 |
| enforce／mahalanobis | 0.1822 | **0.1049** | 無 |

**四組仍然全部未通過 acceptance**，`deployment_eligible=false` 不變。
但每一個數字都不同，而且有三個約束旗標翻面。

⚠️ **值得特別注意的一項退步**：現行資料上，**四組的
`normal_false_unknown_budget` 全部為 false**。舊資料上兩個 permissive 設定
是 true。也就是說在修好證據通道之後，正常流量被誤判為未知的比率反而
超出預算（worst 0.0375／0.0333）。這與
`文件/強OOD單獨判定_不可行_2026-08-28.md` 的結論方向一致：
未知偵測與正常流量的誤判是同一個取捨的兩端。

## 三、P2 conformal 準備度：仍然 blocked，缺額差 1 場

| 模式 | 參考 | 校準場次 | 需要 | 短缺 |
|---|---|---:|---:|---:|
| Permissive | known_attack | **16** | 19 | **3** |
| Permissive | normality | 23 | 19 | 0 ✅ |
| Enforce | known_attack | **17** | 19 | **2** |
| Enforce | normality | 24 | 19 | 0 ✅ |

兩模式皆 `blocked_insufficient_calibration_sessions`、`deployment_eligible=false`。
與舊資料相比各差 1 場（P 由 15→16、E 由 18→17）。

`normality` 兩模式都已滿足，是因為 2026-08-25 依 Jesse 決定把 `--normal-alpha`
由 0.02 改為 0.05。**那是放寬不是改良**：conformal 的 normality 參考現在容許
5% 誤報，而二元閘門仍以 2% 為目標，conformal 層不再是較嚴的那個。
alpha 記在 artifact 裡，任何一份報告都能自證用的是哪一個。

## 四、可稽核性

- 六份新 artifact 全部由 **Codex 自己寫的評估器**產生
  （`evaluate_parallel_gate_loo.py`、`audit_conformal_readiness.py`），
  我沒有修改那兩支腳本，只換輸入。
- 每份 artifact 自己記錄 features 與 metrics 的路徑與 SHA-256。
- 四組 LOO 的 macro unknown recall 與我在 C2C-036 獨立重跑的值
  **逐位相同**（0.0931／0.2709／0.0740／0.1049）；conformal 的校準場次
  （16／23、17／24）亦同。

## 五、對證據總帳的影響

P1 ledger（SHA-256 `0be257c0…d8f2b`）與 P2 ledger（`71b8bd32…ca8be`）
引用的仍是舊 artifact。本文**不修改那兩份帳本**——它們是不可變的
checkpoint，代表產生當時有效。要更新必須另出新 revision ledger，
那屬於 C2C-037 P3 的第 3 項，需要 Codex 的 `project_evidence.py` 流程。

現階段正確的說法是：**P1／P2 的方法結論（四組全未通過、兩模式 conformal
blocked）在現行資料上仍然成立，但帳本裡的那六份數值 artifact 已被取代。**

## 六、產物

| 目錄 | 內容 |
|---|---|
| `文件/P1_AI平行閘門_LOO_2026-08-28_現行資料/` | 四份 LOO（現行資料） |
| `文件/P2_Conformal準備度_2026-08-28_現行資料/` | 兩份 conformal（現行資料） |
| `文件/P1_AI平行閘門_LOO_2026-08-25/` | Codex 原始，保留為歷史 |
| `文件/P2_Conformal準備度_2026-08-25{,_alpha005}/` | Codex 原始，保留為歷史 |
